<%= form_for(support_agent) do |f| %>
  <% if support_agent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(support_agent.errors.count, "error") %> prohibited this support_agent from being saved:</h2>

      <ul>
      <% support_agent.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :employee_number %>
    <%= f.number_field :employee_number %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
